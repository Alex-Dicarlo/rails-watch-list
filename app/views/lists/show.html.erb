<p><%= link_to "Main Page", lists_path, class: "btn btn-danger" %></p>

<% if @list.photo.attached? %>
  <div class="show-container" style="background-image: url('<%= cl_image_path @list.photo.key, width: 400, height: 450, crop: :fill%>')">
<% else  %>
  <div class="show-container" style="background-image: url('https://us.123rf.com/450wm/hugolacasse/hugolacasse1603/hugolacasse160300013/53756518-illustration-of-a-film-stripe-reel-on-abstract-movie-background.jpg?ver=6')">
<% end %>
  <h2><%= @list.name %></h2>
  <p><%= link_to "Add Movie", new_list_bookmark_path(@list), class: "btn btn-danger" %></p>
</div>


<p style="text-align: center;"><strong>Movies</strong></p>
<div class="container">
<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li class="list-group-item">
      <div class="card-product">
      <%= image_tag bookmark.movie.poster_url %>
  <div class="card-product-infos">
  <h6 class="X_btn"><%= link_to "❌", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></h6>
    <h2><%= bookmark.movie.title %></h2>
    <p>  <%= bookmark.movie.overview %></p>
    <h6> Comment Section </h6>
    <p> <%= bookmark.comment %> 🗣 </p>
  </div>
</div>
    </li>
  <% end %>
</ul>
<div>
